# sShell

The backbone of SinhaOS a debian based distro

## Why are we using sShell

The common reason for using sShell is to make it possible to add special systems on top of sShell to handle emitting code to embedded systems